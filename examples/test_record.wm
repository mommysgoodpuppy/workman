record Point { x: Int, y: Int, z: String };

let test = () => {
  let f = (p) => { .{ x: p.x, y: p.y } };
  f
};

let main = () => {
  let g = test();
  g({ x: 10 })
};

record R { foo: Int, bar:Int };
let thing = {
  foo: 1,
};