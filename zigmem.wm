--const std = @import("std");
from "std/mem" import {alloc, free};

let main = () => {

  let buffer = alloc(100);

  --buffer[0] = 'H';
  --buffer[1] = 'i';

  --std.debug.print("Buffer says: {s}\n", .{buffer[0..2]});
  free(buffer);
}