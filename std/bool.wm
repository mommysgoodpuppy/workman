export let not = match(value) {
  true => { false },
  false => { true }
};

export let boolAnd = match(tuple) {
  (true, true) => { true },
  _ => { false }
};

export let boolOr = match(tuple) {
  (false, false) => { false },
  _ => { true }
};

export let boolXor = match(tuple) {
  (true, true) => { false },
  (false, false) => { false },
  _ => { true }
};

export let ifThenElse = match(tuple) {
  (true, thenBranch, _) => { thenBranch() },
  (false, _, elseBranch) => { elseBranch() }
};
